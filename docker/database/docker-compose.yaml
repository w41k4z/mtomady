services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: mtomady
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data: